# V4 content masks

White pixels remain in geometry review. Black pixels identify only permitted dynamic content: captured device chrome and, where present, raster photo interiors. Controls, typography, container boundaries, spacing, map polygons and selected-card geometry are never masked. All boards, alpha overlays, heatmaps and reported unmasked RGB differences are generated from the original images without applying these masks.
