function HomeScreen({ t, onLocation }: { t: MobileCopy; onLocation: () => void }) {
  const { rentalMode, setRentalMode } = useApp()
  return <section className="m2-screen m2-home"><header className="m2-topbar"><Brand compact /></header><div className="m2-hero" role="img" aria-label={t.heroAlt} /><div className="m2-search-card"><div className="m2-mode-switch" role="group" aria-label={`${t.housingMode} / ${t.tourismMode}`}><button type="button" className={cn(rentalMode === 'long' && 'is-active')} onClick={() => setRentalMode('long')} aria-pressed={rentalMode === 'long'}><span className="m2-mode-icon m2-mode-icon--home"><Home /></span><span>{t.housingMode}</span></button><button type="button" className={cn(rentalMode === 'holiday' && 'is-active')} onClick={() => setRentalMode('holiday')} aria-pressed={rentalMode === 'holiday'}><span className="m2-mode-icon m2-mode-icon--tourism"><BriefcaseBusiness /></span><span>{t.tourismMode}</span></button></div><OccupantSelector t={t} /><button type="button" className="m2-select-row" onClick={onLocation}><span>{t.searchTenerife}</span><MapPin /></button><PrimaryButton onClick={onLocation} testId="open-location"><Search />{t.search}</PrimaryButton><button type="button" className="m2-outline">{t.publishAd}</button></div></section>
}